# СПИСОК ИЗ КУБОВ (генерация списка, цикл for)
# Используя списочное выражение и метод split, составьте список из введенных чисел,
# записанных на одной строке без указания заранее их количества; затем выведите их квадраты также на одной строке.

a=map(int, input().split()) # создаем список из неопределенного количества целых чисел int
b=[i**3 for i in a] # в переменную b генерируем список кубов чисел из списка a
for i in b: # перебором выводим каждый элемент списка b
   print(i, end=' ') # для того чтоб результат выводился одной строкой, заменяем разрыв строки пробелом
