# Find Numbers with Even Number of Digits
# Дано список из чисел, необходимо найти длину каждого элемента (числа) и проверить его на четность.
# Вывести необходимо количество елементов с четной длиной

def even(a): # создаем функцию, в нее будет вводится одна переменная - весь наш список
    x = [] # создаем пустой список который будем наполнять количеством четных чисел
    for i in range(5):
        if len(str(a[i]))%2==0: #проверяем длину каждого числа на четность 
            # (берем элемент по индексу и подставляем его строчное значение)
            x.append(i) # если длина числа четная - в список добавится индекс этого числа
    print(len(x)) # выводим количество четных - длина списка из индексов четных элементов




x=[12,345,2,6,7896]
even(x) # применяем функцию к нашей переменной(списку чисел)
