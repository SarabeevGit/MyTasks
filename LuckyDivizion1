# Счастливое деление (задача на множества, set, цикл)
# Петя любит счастливые числа. Всем известно, что счастливыми являются положительные целые числа, в десятичной 
# записи которых содержатся только счастливые цифры 4 и 7. Например, числа 47, 744, 4 являются счастливыми, 
# а 5, 17, 467 — не являются.
# Петя называет число почти счастливым, если оно делится без остатка на какое-либо счастливое число. Помогите ему 
# узнать, является ли заданное число n почти счастливым?
# Входные данные
# В единственной строке задано целое число n (1 ≤ n ≤ 1000) — число, которое нужно проверить.
# Выходные данные
# В единственной строке выведите «YES» (без кавычек), если число n почти счастливое, иначе выведите «NO» (без кавычек).
  # Подготовкаа к решению: создаем множество, которое содержит числа от 1 до 1000
  # и которые состоят только из нужных нам цифр (4,7)

x = [i for i in range(1, 1000)] #создаем болванку для перебора
zz = {4} #создаем множество
z = '444777474747' #список с которым будем сравнивать
for i in x: #цикл обходит болванку  и нужные добавляет в наше множество
   if str(i) in z: #сравнивает символы со списком z
       zz.add(i) #добавляет нужные числа во множество

a = input() #вводим заданное число
b = set((a)) #разбиваем число на множество для сравнения
z = 0
# проверяем число на вхождение в заданные параметры (в цикл zz)
if b != x: #если не входит, то проверяем на деление числа
           # на одно из счастливых чисел из цикла zz
   for i in zz:
       if int(a) % int(i) == 0: #если число делится хотя бы на одно число из множества zz
           z += 1 #то переменная z будет больше ноля
if z > 0 or b == zz: #если число Счастливое или его делится на Счастливое
   print('YES')
else:
   print('NO')
